This is a code injector code without UI, you can take this and add UI too. This script leverages Scapy and NetfilterQueue to intercept unencrypted HTTP traffic and dynamically inject a custom JavaScript payload into the HTML responses. It ensures the modification is processed correctly by stripping compression headers from outgoing requests and automatically recalculating the Content-Length header to account for the injected code.
